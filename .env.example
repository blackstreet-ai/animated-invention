# ------------------------------------------------------------------
# Environment variables for AI Video Essay Pipeline (MCP edition)
# ------------------------------------------------------------------
# Copy this file to `.env` and fill in the blanks. *Never* commit the
# filled `.env` to version control – it is ignored via `.gitignore`.

# --- YouTube MCP ----------------------------------------------------
# Obtain a YouTube Data API v3 key from Google Cloud Console:
# 1. Create (or select) a GCP project
# 2. Enable "YouTube Data API v3"
# 3. Create an API key in the Credentials tab
# 4. Restrict the key by IP or HTTP referer if desired
# ------------------------------------------------------------------
YOUTUBE_API_KEY=""

# Optional: override default port (5010)
YOUTUBE_MCP_PORT=5010

# --- Global timeouts / retries (seconds) ---------------------------
MCP_REQUEST_TIMEOUT=30
MCP_HEALTHCHECK_INTERVAL=60
